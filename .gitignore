# OS
.DS_Store
Thumbs.db

# Editors
.vscode/
.idea/

# Python (future CLI)
__pycache__/
*.py[cod]
.venv/
venv/
*.egg-info/

# Node (future web tool)
node_modules/
npm-debug.log
.env
.env.local

# Local country submissions and their outputs — never commit
samples/private/
*.xlsx
*.docx
*.pdf
!docs/**/*.pdf

# Overlay deliverables kept under samples/<country>-<yyyy-mm>/ are the audit
# trail of each review. Track them even though .docx is excluded above.
!samples/**/*.docx
!samples/**/*.png

# Calibration runs that contain unpublished country data
calibration/private/
